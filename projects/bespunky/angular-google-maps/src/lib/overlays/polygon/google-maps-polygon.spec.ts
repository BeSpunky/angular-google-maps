import { GoogleMapsPolygon } from './google-maps-polygon';

describe('GoogleMapsPolygon', () => {
  it('should create an instance', () => {
    expect(new GoogleMapsPolygon()).toBeTruthy();
  });
});
