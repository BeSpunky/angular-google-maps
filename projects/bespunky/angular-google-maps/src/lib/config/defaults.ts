import { ZoomLevel } from '../types/zoom-level.enum';

export class Defaults
{
    public static readonly Center = { lat: 31.852775, lng: 34.818484 };
    public static readonly ZoomLevel = ZoomLevel.City;
}
